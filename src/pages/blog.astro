---
const allpost = await Astro.glob("./posts/*.md*");
---


    <ul>
        {
            allpost.map((post) => {
                return (
                    <li>
                        <a href={post.url}>{post.frontmatter.title}</a>
                    </li>
                );
            })
        }
    </ul>
